79. Create a PHP program to append two files in another file.
code:
<?php
// Names of the source files
$file1 = "file1.txt";
$file2 = "file2.txt";

// Name of the destination file
$destinationFile = "combined.txt";

// Check if source files exist
if(!file_exists($file1) || !file_exists($file2)) {
    die("One or both source files do not exist.");
}

// Read contents of both files
$content1 = file_get_contents($file1);
$content2 = file_get_contents($file2);

// Combine contents
$combinedContent = $content1 . PHP_EOL . $content2;

// Write combined content into destination file
if(file_put_contents($destinationFile, $combinedContent)) {
    echo "Files '$file1' and '$file2' have been appended into '$destinationFile'.";
} else {
    echo "Error: Could not write to '$destinationFile'.";
}
?>
